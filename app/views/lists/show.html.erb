<h1><%= @list.name %></h1>

<h2>Movies</h2>
<%= link_to 'Add a movie', bookmarks_new_path(@bookmark) %>
<ul class="list-group">
  <% @list.movies.each do |movie| %>
    <li class="list-group-item">
      <%= movie.title %>
    </li>
  <% end %>
</ul>
